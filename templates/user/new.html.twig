{# templates/user/new.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Create New User{% endblock %}

{% block body %}
    <h1>Create New User</h1>

    {# Render the form #}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary mt-2">Create User</button>
    {{ form_end(form) }}

    {# Back to the user list #}
    <a href="{{ path('app_user_index') }}" class="btn btn-secondary mt-3">Back to Users List</a>
{% endblock %}
